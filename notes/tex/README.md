# LaTeX source files

Put standalone `.tex` source files in this folder. Link to them from a `.qmd` or `.md` note, for example:

```markdown
[Download the LaTeX source](tex/my-note.tex)
```

Quarto renders Markdown/Quarto notes as web pages; standalone `.tex` files remain downloadable source files.

